// import { Router } from 'express';
// import { expressValidator } from '../../middleware/expressValidator';

// const router = Router();

// router.get('/:enterpriseId/stop-group-sub-group', expressValidator(stopGroupSchemas.getAll), stopGroupControllers.getAll);
// router.get('/:enterpriseId/stop-group', expressValidator(stopGroupSchemas.get), stopGroupControllers.get);
// router.post('/:enterpriseId/stop-group', expressValidator(stopGroupSchemas.create), stopGroupControllers.create);
// router.put('/stop-group/:groupId', expressValidator(stopGroupSchemas.update), stopGroupControllers.update);
// router.delete('/stop-group/:groupId', expressValidator(stopGroupSchemas.remove), stopGroupControllers.remove);

// export default router; 